---
import Layout from '../layouts/Layout.astro';
import { client, urlFor } from '../lib/sanity';
import type { Project, Post } from '../lib/sanity-types';

// Fetch ultimi 3 progetti
const projects = await client.fetch<Project[]>(`
  *[_type == "project"] | order(_createdAt desc)[0...3] {
    _id,
    title,
    description,
    country,
    duration,
    heroImage,
    "slug": slug.current
  }
`);

// Fetch ultime 3 news
const posts = await client.fetch<Post[]>(`
  *[_type == "post"] | order(pubDate desc)[0...3] {
    _id,
    title,
    description,
    pubDate,
    heroImage,
    "slug": slug.current
  }
`);
---

<Layout title="Progetto Erasmus - Istituto Marconi" description="Scopri le opportunità di mobilità internazionale del nostro progetto Erasmus presso l'Istituto Marconi">
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-r from-blue-600 via-blue-700 to-blue-800 text-white py-24 overflow-hidden">
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="container mx-auto px-4 text-center relative z-10">
      <div class="max-w-4xl mx-auto">
        <div class="flex justify-center mb-8">
          <img src="/erasmus-logo-white.png" alt="Erasmus Logo" class="h-20 w-auto" />
        </div>
        <h1 class="text-5xl md:text-7xl font-bold mb-6 leading-tight">
          Progetto Erasmus+
        </h1>
        <p class="text-2xl md:text-3xl mb-8 text-blue-100 font-light">
          Istituto Marconi - Mobilità Internazionale
        </p>
        <p class="text-lg md:text-xl mb-12 text-blue-200 max-w-3xl mx-auto">
          Partecipa alla celebrazione internazionale del programma Erasmus+ e scopri le opportunità di studio e formazione all'estero
        </p>
        <div class="flex flex-col sm:flex-row gap-6 justify-center">
          <a href="/progetti" class="bg-yellow-400 text-blue-900 px-8 py-4 rounded-lg font-bold text-lg hover:bg-yellow-300 transition-colors shadow-lg">
            Scopri i Progetti
          </a>
          <a href="/contatti" class="border-2 border-white text-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-white hover:text-blue-900 transition-colors">
            Contattaci
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ErasmusDays Banner -->
  <section class="bg-yellow-400 py-8">
    <div class="container mx-auto px-4 text-center">
      <div class="flex flex-col md:flex-row items-center justify-center space-y-4 md:space-y-0 md:space-x-8">
        <div class="flex items-center space-x-4">
          <div class="w-12 h-12 bg-blue-600 rounded-full flex items-center justify-center">
            <span class="text-white font-bold text-xl">9</span>
          </div>
          <div class="text-left">
            <p class="text-blue-900 font-bold text-lg">9ª Edizione</p>
            <p class="text-blue-800 text-sm">ErasmusDays 2025</p>
          </div>
        </div>
        <div class="text-blue-900">
          <p class="font-bold text-lg">13-18 Ottobre 2025</p>
          <p class="text-sm">6 giorni per far brillare l'Europa!</p>
        </div>
        <a href="https://www.erasmusdays.eu/" target="_blank" rel="noopener" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
          Partecipa agli ErasmusDays
        </a>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-6 text-gray-800">
          Perché Scegliere il Progetto Erasmus?
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Il programma Erasmus+ offre opportunità uniche per studenti, insegnanti e personale dell'Istituto Marconi
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-white p-8 rounded-xl shadow-lg hover:shadow-xl transition-shadow border-l-4 border-blue-600">
          <div class="w-16 h-16 bg-blue-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-gray-800">Mobilità Internazionale</h3>
          <p class="text-gray-600 leading-relaxed">
            Studia in università partner europee e sviluppa competenze interculturali uniche. 
            Esplora nuove culture e amplia i tuoi orizzonti.
          </p>
        </div>
        
        <div class="bg-white p-8 rounded-xl shadow-lg hover:shadow-xl transition-shadow border-l-4 border-yellow-400">
          <div class="w-16 h-16 bg-yellow-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-gray-800">Formazione di Qualità</h3>
          <p class="text-gray-600 leading-relaxed">
            Accedi a programmi di studio innovativi e metodologie didattiche all'avanguardia. 
            Migliora le tue competenze professionali.
          </p>
        </div>
        
        <div class="bg-white p-8 rounded-xl shadow-lg hover:shadow-xl transition-shadow border-l-4 border-green-500">
          <div class="w-16 h-16 bg-green-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-semibold mb-4 text-gray-800">Rete Internazionale</h3>
          <p class="text-gray-600 leading-relaxed">
            Costruisci relazioni professionali e personali con studenti e docenti di tutta Europa. 
            Crea connessioni durature per il tuo futuro.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Statistics Section -->
  <section class="py-20 bg-blue-600 text-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold mb-6">
          ErasmusDays 2024: Risultati Straordinari
        </h2>
        <p class="text-xl text-blue-100">
          Unisciti alla celebrazione internazionale del programma Erasmus+
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8 text-center">
        <div class="bg-white/10 p-8 rounded-xl backdrop-blur-sm">
          <div class="text-5xl font-bold text-yellow-400 mb-4">10,112</div>
          <h3 class="text-xl font-semibold mb-2">Eventi Organizzati</h3>
          <p class="text-blue-100">In tutto il mondo nel 2024</p>
        </div>
        
        <div class="bg-white/10 p-8 rounded-xl backdrop-blur-sm">
          <div class="text-5xl font-bold text-yellow-400 mb-4">6</div>
          <h3 class="text-xl font-semibold mb-2">Giorni di Celebrazione</h3>
          <p class="text-blue-100">Per far brillare l'Europa</p>
        </div>
        
        <div class="bg-white/10 p-8 rounded-xl backdrop-blur-sm">
          <div class="text-5xl font-bold text-yellow-400 mb-4">9ª</div>
          <h3 class="text-xl font-semibold mb-2">Edizione 2025</h3>
          <p class="text-blue-100">13-18 Ottobre</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Projects Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="flex justify-between items-center mb-12">
        <div>
          <h2 class="text-4xl font-bold mb-4 text-gray-800">
            I Nostri Progetti
          </h2>
          <p class="text-xl text-gray-600">
            Scopri le esperienze di mobilità internazionale
          </p>
        </div>
        <a href="/progetti" class="hidden md:inline-block bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
          Vedi Tutti
        </a>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {projects.map((project) => (
          <article class="bg-white rounded-xl shadow-lg hover:shadow-xl transition-all border-l-4 border-blue-600 overflow-hidden">
            {project.heroImage && (
              <img 
                src={urlFor(project.heroImage).width(600).height(300).url()} 
                alt={project.heroImage.alt || project.title}
                class="w-full h-48 object-cover"
              />
            )}
            <div class="p-6">
              <div class="flex gap-2 mb-3">
                <span class="bg-blue-100 text-blue-800 text-xs font-semibold px-3 py-1 rounded-full">
                  {project.country}
                </span>
                <span class="bg-gray-100 text-gray-800 text-xs font-semibold px-3 py-1 rounded-full">
                  {project.duration}
                </span>
              </div>
              <h3 class="text-xl font-bold mb-3 text-gray-800 line-clamp-2">
                {project.title}
              </h3>
              <p class="text-gray-600 mb-4 line-clamp-3">
                {project.description}
              </p>
              <a 
                href={`/progetti/${project.slug}`} 
                class="inline-block bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-blue-700 transition-colors"
              >
                Scopri di più
              </a>
            </div>
          </article>
        ))}
      </div>
      
      <div class="text-center mt-12 md:hidden">
        <a href="/progetti" class="inline-block bg-blue-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
          Vedi Tutti i Progetti
        </a>
      </div>
    </div>
  </section>

  <!-- News Section -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="flex justify-between items-center mb-12">
        <div>
          <h2 class="text-4xl font-bold mb-4 text-gray-800">
            Ultime News
          </h2>
          <p class="text-xl text-gray-600">
            Rimani aggiornato sulle nostre attività
          </p>
        </div>
        <a href="/blog" class="hidden md:inline-block bg-purple-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-purple-700 transition-colors">
          Vedi Tutte
        </a>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        {posts.map((post) => (
          <article class="bg-white rounded-xl shadow-lg hover:shadow-xl transition-all border-l-4 border-purple-600 overflow-hidden">
            {post.heroImage && (
              <img 
                src={urlFor(post.heroImage).width(600).height(300).url()} 
                alt={post.heroImage.alt || post.title}
                class="w-full h-48 object-cover"
              />
            )}
            <div class="p-6">
              <time class="text-sm text-purple-600 font-semibold mb-3 block">
                {new Date(post.pubDate).toLocaleDateString('it-IT', {
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric'
                })}
              </time>
              <h3 class="text-xl font-bold mb-3 text-gray-800 line-clamp-2">
                {post.title}
              </h3>
              <p class="text-gray-600 mb-4 line-clamp-3">
                {post.description}
              </p>
              <a 
                href={`/blog/${post.slug}`} 
                class="inline-block bg-purple-600 text-white px-4 py-2 rounded-lg font-semibold hover:bg-purple-700 transition-colors"
              >
                Leggi Articolo
              </a>
            </div>
          </article>
        ))}
      </div>
      
      <div class="text-center mt-12 md:hidden">
        <a href="/blog" class="inline-block bg-purple-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-purple-700 transition-colors">
          Vedi Tutte le News
        </a>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-r from-yellow-400 to-yellow-500">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-4xl font-bold mb-6 text-blue-900">
        Pronto per la Tua Avventura Erasmus?
      </h2>
      <p class="text-xl mb-12 text-blue-800 max-w-3xl mx-auto">
        Scopri le opportunità disponibili presso l'Istituto Marconi e inizia il tuo percorso di mobilità internazionale.
      </p>
      <div class="flex flex-col sm:flex-row gap-6 justify-center">
        <a href="/progetti" class="bg-blue-600 text-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-blue-700 transition-colors shadow-lg">
          Vedi Tutti i Progetti
        </a>
        <a href="/contatti" class="bg-white text-blue-900 px-8 py-4 rounded-lg font-bold text-lg hover:bg-blue-50 transition-colors shadow-lg">
          Contattaci Ora
        </a>
      </div>
    </div>
  </section>

  <!-- EU Funding Disclaimer -->
  <section class="py-12 bg-white border-t border-gray-200">
    <div class="container mx-auto px-4">
      <div class="max-w-5xl mx-auto">
        <div class="flex flex-col md:flex-row items-center md:items-start gap-8">
          <div class="flex-shrink-0">
            <img 
              src="/EN_Co-fundedbytheEU_RGB_POS.png" 
              alt="Co-funded by the European Union" 
              class="h-20 md:h-24 w-auto"
            />
          </div>
          <div class="text-center md:text-left">
            <p class="text-sm md:text-base text-gray-700 leading-relaxed">
              <strong>Finanziato dall'Unione europea.</strong> Le opinioni espresse appartengono, tuttavia, al solo o ai soli autori e non riflettono necessariamente le opinioni dell'Unione europea o dell'Agenzia esecutiva europea per l'istruzione e la cultura (EACEA). Né l'Unione europea né l'EACEA possono esserne ritenute responsabili.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>